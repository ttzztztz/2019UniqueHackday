import { createStyles } from "@material-ui/core/styles";

const styles = createStyles({
    footer: {
        textAlign: "center",
        marginTop: "48px"
    },
    "footer-nav": {
        height: "48px",
        borderRadius: "24px",
        background: "#FD504D",
        textAlign: "center",
        color: "white",
        fontSize: "16px",
        padding: "12px 0",
        margin: "12px auto",
        width: "75%"
    }
});

export default styles;
