import { createStyles } from "@material-ui/core/styles";

const styles = createStyles({
    main: {
        height: "120px",
        width: "70%",
        textAlign: "center",
        margin: "18px auto",
        fontSize: "36px"
    }
});

export default styles;
