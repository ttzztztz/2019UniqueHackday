import { createStyles } from "@material-ui/core/styles";

const styles = createStyles({
    table: {
        margin: "24px auto"
    },
    "table-head": {
        fontWeight: "bold"
    },
    "table-item": {
        height: "64px"
    }
});

export default styles;
