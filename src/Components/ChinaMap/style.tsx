import { createStyles } from "@material-ui/core/styles";

const styles = createStyles({
    "map-layer": {
        position: "fixed",
        borderRadius: "6px",
        width: "175px",
        height: "72px",
        padding: "8px",
        background: "#fd504d",
        opacity: 0.7,
        color: "white",
        userSelect: "none"
    }
});

export default styles;
