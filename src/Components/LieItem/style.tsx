import { createStyles } from "@material-ui/core/styles";

const styles = createStyles({
    main: {
        width: "70%",
        textAlign: "center",
        margin: "18px auto"
    },
    "ok-icon": {
        color: "green"
    },
    "no-icon": {
        color: "red"
    }
});

export default styles;
