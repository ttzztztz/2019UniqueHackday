import { createStyles } from "@material-ui/core/styles";

const styles = createStyles({
    root: {
        height: "28px",
        textAlign: "center",
        borderRadius: "16px",
        padding: "3px 12px",
        display: "inline-block",
        width: "auto",
        border: "#fd504d 1px solid"
    }
});

export default styles;
