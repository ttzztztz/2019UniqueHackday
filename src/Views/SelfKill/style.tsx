import { createStyles } from "@material-ui/core/styles";

const styles = createStyles({
    main: {
        width: "75%",
        margin: "36px auto"
    },
    "avatar-container": {
        textAlign: "center",
        margin: "48px auto"
    }
});

export default styles;
