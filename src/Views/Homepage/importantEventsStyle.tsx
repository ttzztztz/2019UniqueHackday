import { createStyles } from "@material-ui/core/styles";

const styles = createStyles({
    bar: {
        background: "#E4EDFF",
        color: "black",
        boxShadow: "0px 0px"
    },
    "bar-container": {
        background: "#E4EDFF"
    }
});

export default styles;
